// Simple encryption utilities for API keys
// In production, use proper encryption libraries

export function decryptApiKey(encryptedKey: string): string {
  // Stub implementation - in production, use proper decryption
  // For now, assume keys are stored in plain text (not recommended for production)
  return encryptedKey;
}

export function encryptApiKey(apiKey: string): string {
  // Stub implementation - in production, use proper encryption
  return apiKey;
}



